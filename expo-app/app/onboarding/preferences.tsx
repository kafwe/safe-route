import { View } from "react-native";
import { Text } from "react-native-paper";

export default function PreferencesOnboarding() {
	return (
		<View>
			<Text>PreferencesOnboarding</Text>
		</View>
	);
}
